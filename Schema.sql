CREATE TABLE Pending_Tasks(
    id VARCHAR(100) not null,
    content  VARCHAR(200),
    Task_date  VARCHAR(100)  
);

CREATE TABLE Complete_Tasks(
    id VARCHAR(100) not null,
    content  VARCHAR(200),
    Task_date  VARCHAR(100)  
);
